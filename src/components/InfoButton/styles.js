import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
	wrap: {
		width: 40,
		height: 40,
	},

	button: {
		alignItems: 'center',
	},
	text: {
		lineHeight: 32,
		color: '#fff',
		fontSize: 20,
	},
});

export default styles;