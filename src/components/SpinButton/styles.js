import {StyleSheet} from 'react-native';

import {SPIN_BUTTON_HEIGHT, SPIN_BUTTON_WIDTH} from '../../constants';

const styles = StyleSheet.create({
	wrap: {},
	
	img: {
		height: SPIN_BUTTON_HEIGHT,
		width: SPIN_BUTTON_WIDTH,
	},
});

export default styles;