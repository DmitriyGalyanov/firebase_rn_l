import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
	wrap: {
		height: 50,
		width: 100,
	},

	circleWrap: {
		position: 'absolute',
	},

	circle: {
		height: 60,
		width: 60,
	},
});

export default styles;