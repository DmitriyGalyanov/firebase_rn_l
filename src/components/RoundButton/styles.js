import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
	wrap: {
		width: 60,
		height: 60,
		alignItems: 'center',
	},

	button: {},
	
	text: {
		lineHeight: 48,
		color: '#fff',
		fontSize: 30,
	},
});

export default styles;